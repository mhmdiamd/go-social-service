FROM postgres:alpine3.20

# Set default environment variables
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=password
ENV POSTGRES_DB=db_social_service

# Expose the default PostgreSQL port
EXPOSE 5432
